# FinanCell branding assets

These PNG files are source assets for the upcoming application UI. They are
intentionally version-controlled and are not generated build output.

Keep new exported variants clearly named and remove obsolete variants when the
UI chooses its final icon, logo, banner, and monochrome treatments.
